# CLAUDE.md

## 프로젝트 개요

이 프로젝트는 마인크래프트 스타일의 샌드박스 게임을 만드는 프로젝트이다.

핵심 목표:

* 블록 기반 월드
* 청크 시스템
* 블록 설치/파괴
* 인벤토리
* 플레이어 이동
* 저장 시스템
* 최적화된 렌더링
* 멀티플레이 확장 가능 구조

코드는 유지보수성과 성능을 가장 중요하게 고려한다.

---

# 중요 원칙

## 1. 기존 기능 절대 망가뜨리지 말 것

새 기능 추가 시:

* 기존 기능 유지
* 기존 API 유지
* 기존 UI 유지
* 기존 저장 데이터 호환 유지

기존 기능을 수정해야 하면:

1. 이유 설명
2. 영향 설명
3. 대체 방법 설명

---

## 2. 전체 파일을 함부로 재작성하지 말 것

가능하면:

* 필요한 부분만 수정
* 최소 변경 원칙 유지

절대:

* 멀쩡한 코드 전체 재생성 금지

---

# 마인크래프트 스타일 시스템 규칙

## 월드 시스템

월드는 청크 기반으로 구현한다.

청크 규칙:

* 청크 크기 상수화
* 보이는 청크만 렌더링
* 거리 기반 로딩
* 거리 기반 언로딩
* 메모리 누수 방지

---

## 블록 시스템

블록 데이터는:

* id
* 이름
* 텍스처
* 충돌 여부
* 투명 여부
* 내구도

를 기본 포함한다.

블록 추가 시:

* enum 또는 registry 사용
* 하드코딩 최소화

---

## 렌더링 규칙

항상 성능 우선:

* 불필요한 draw 호출 금지
* 컬링 적용
* 청크 메쉬 재사용
* requestAnimationFrame 사용

가능하면:

* GPU 친화적 구조 사용

---

## 플레이어 시스템

플레이어 기능:

* 이동
* 점프
* 중력
* 충돌 처리
* 시점 회전

추가 시:

* 물리 연산 분리
* 입력 처리 분리

---

## 인벤토리 시스템

인벤토리는:

* 슬롯 기반
* 드래그 가능 구조 고려
* 아이템 스택 지원
* 최대 스택 크기 지원

---

## 저장 시스템

저장 시:

* JSON 구조 명확히 유지
* 버전 정보 포함
* 손상 방지 고려

---

# 디버깅 규칙

버그 수정 시:

1. 원인 먼저 분석
2. 최소 수정으로 해결
3. 관련 부작용 검사

추측으로 수정하지 말 것.

---

# 응답 규칙

코드 수정 시:

* 무엇을 수정했는지 설명
* 왜 수정했는지 설명
* 영향 범위 설명

가능하면:

* 수정 전/후 차이 설명

---

# 성능 규칙

항상 고려:

* FPS
* 메모리 사용량
* 청크 렌더링 비용
* 텍스처 로딩 비용

대규모 월드에서도 동작 가능하도록 작성.

---

# 금지 사항

절대 하지 말 것:

* 사용자가 요청하지 않은 대규모 리팩토링
* 임의 파일 삭제
* 필요 없는 의존성 추가
* 테스트 안 된 코드라고 숨기기
* 의미 없는 콘솔 로그 남발
* 성능 악화시키는 구조 추가

---

# 선호 기술

우선 선호:

* TypeScript
* WebGL
* Three.js
* ECS 구조
* 모듈화 구조

---

# 작업 우선순위

우선순위:

1. 안정성
2. 성능
3. 유지보수성
4. 기능 추가
5. 시각 효과

---

# 목표

최종 목표는:

* 실제 플레이 가능한 수준의 마인크래프트 스타일 게임
* 최적화된 구조
* 확장 가능한 코드베이스
* 장기 개발 가능한 아키텍처

이다.

---

# 현재 구조 (2026-07 재작성)

렌더링을 "블록 하나 = 인스턴스" 방식에서 **청크 메시 + 텍스처 아틀라스** 방식으로 교체했다.
보이는 면만 삼각형으로 만들기 때문에 같은 블록 수에서 드로우 콜과 정점 수가 크게 줄었다.

```
index.html / style.css   HUD·인벤토리·메뉴 DOM
main.js                  게임 루프, 입력, 블록 상호작용, 아이템 드롭, 손에 든 아이템
src/textures.js          절차적 16x16 픽셀아트 아틀라스 (외부 이미지 없음)
src/blocks.js            블록·아이템·조합법·제련 레지스트리, 채굴 시간 공식
src/worldgen.js          노이즈, 바이옴, 지형 스플라인, 동굴, 광석, 나무/식물
src/chunk.js             청크 저장(Uint8Array) + 면 컬링 메시 + AO (조명 값은 월드에서 읽어 씀)
src/world.js             청크 로드/언로드, 블록 수정, DDA 레이캐스트, **월드 단위 조명 전파**
src/commands.js          채팅 · 명령어 (/help, /tp, /gamemode, /give, /time, /dimension …)
src/dimensions.js        네더 · 엔드 지형 생성기와 차원별 설정
src/portals.js           네더 차원문 판정/생성, 엔드 차원문, 차원 이동
src/player.js            AABB 충돌, 이동/점프/수영/웅크리기, 체력·허기·산소
src/inventory.js         아이템 스택, 제작 격자, 화로
src/ui.js                핫바·인벤토리·제작대·화로·HUD
src/env.js               하늘, 낮/밤 사이클, 구름, 안개
src/mobs.js              동물/적대 몹, 배회·추적 AI
src/audio.js             WebAudio 절차적 효과음
src/save.js              저장/불러오기 (v3, 구버전 v2 읽기 호환)
```

## 지켜야 할 규칙

* 블록은 `(x, y, z)` 에서 `[x, x+1]` 범위를 차지한다 (마인크래프트와 동일).
* **y 는 -64 ~ 191** 이다. 배열 인덱스는 `idx(x, y, z)` 가 알아서 보정하므로 월드 y 를 그대로 넘기면 된다.
  범위 검사는 `y < MIN_Y || y > MAX_Y` 로 한다 (`y < 0` 이 아니다).
* 차원은 `World.setDimension()` 으로 바꾼다. 지형은 시드에서 다시 생성되고
  플레이어 변경분만 `modsByDim` 에 차원별로 보관된다.
* 새 블록은 `blocks.js` 의 `def()` 로만 추가한다. 텍스처는 `textures.js` 의 `TEX` 에 등록한다.
* 블록 데이터가 바뀌면 해당 청크와 (경계라면) 이웃 청크의 `dirty` 를 세워야 메시가 갱신된다.
* 조명은 메시에 구워 넣지 않고 정점 속성(`alight`)으로 넘긴다. 낮/밤은 `skyBrightness` 유니폼만 바꾼다.
* **조명은 반드시 월드 단위로 전파한다.** 청크 안에서만 계산하면 횃불 빛이 청크 경계에서 잘린다.
  `chunk.light` 는 상위 4비트=하늘빛, 하위 4비트=블록빛이고, 갱신은 `World._lightAdd/_lightRemove`
  (BFS)와 `_recalcSkyColumn` 이 담당한다. 블록을 바꾸면 `_updateLight` 가 자동으로 불린다.
* 물·잎은 `blocksSky: true` 라서 하늘빛이 그대로 통과하지 않는다 (깊은 물속·나무 밑이 어두워진다).
* 저장 포맷을 바꿀 때는 `version` 을 올리고 이전 버전 읽기 경로를 남긴다.

## 실제 마인크래프트를 따른 수치

* 걷기 4.317 m/s · 달리기 5.612 m/s · 웅크리기 1.295 m/s · 점프 높이 1.25블록
* 채굴 시간 = 경도 × (알맞은 도구 1.5 / 그 외 5) ÷ 도구 속도(나무2·돌4·철6·다이아8·금12)
* 하루 24000틱 = 20분, 해수면 y=62, 월드 높이 128
* 적대 몹은 블록빛 0 인 어두운 곳(밤의 지표·동굴)에, 동물은 밝은 잔디 위에 생성
* 네더 좌표는 오버월드의 1/8, 차원문 내부는 최소 2×3 흑요석 틀
* 심층암은 y < 0, 광석은 그 구간에서 심층암 광석으로 바뀐다
